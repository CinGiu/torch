# Questo file è solo un esempio — non viene usato da docker-compose.
# Tutta la configurazione avviene dalla UI su http://localhost:8080
# e viene salvata in /data/config.json (volume Docker).

# L'unica variabile che potresti voler sovrascrivere:
# REDIS_ADDR=redis:6379
