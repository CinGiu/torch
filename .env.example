# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Your email address — grants access to the Admin tab
ADMIN_EMAIL=you@example.com

# Restrict login to this domain (e.g. cubbit.io); leave empty to allow any email
ALLOWED_DOMAIN=
